name: eventlogtester

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: sudo apt-get update
      - run: sudo apt-get -y install libefivar-dev pylint python3-pip tpm2-tools black
      - run: sudo pip3 install pyright yq

      # syntax checking
      - name: Run black
        run: black --check      

      # step 1: run pylint (static analysis)
      - name: Run pylint (static analysis)
        run: pylint --rcfile pylintrc eventlog/eventlog.py
      
      # step 2: run pyright (checking type hints)
      - name: Run pyright (checking type hints)
        run: pyright

      # step 3: run event log validation (check event log consistency)
      - name: Run event log validation
        run: true

      # step 4: run comparisons with tpm2-tools output
      - name: Run comparisons with tpm2-tools output
        run: true
